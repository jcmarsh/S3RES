#ifndef _FORCE_H_
#define _FORCE_H_

#include <sys/mman.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

//int parseLine(char* line, ssize_t length);

//int printMaps( );

int lockItUp( );
int forceMaps( );

#endif // _FORCE_H_
