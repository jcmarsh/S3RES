Need to have Empty controllers of different sizes to test restart times.

To check the size of a running program, `ps -a` for the pid, them use `pmap`. So for these files, run `EmptyTest` in the `test` directory, then the executable there along with a piority: `./Empty 20`

